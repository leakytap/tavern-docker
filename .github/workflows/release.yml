name: Release Workflow
on:
  release:
    types: [published]

jobs:
  release:
    env:
      REF: ${{ github.ref }}
    name: Release Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Build
        run: |
          docker build \
            --file ./build/Dockerfile \
            -t ghcr.io/leakytap/tavern-docker:${REF##*/} \
            -t ghcr.io/leakytap/tavern-docker:latest .

      - name: Push
        run: |
          docker login ghcr.io -u Jesse0Michael -p ${{ secrets.PAT }}
          docker push ghcr.io/leakytap/tavern-docker:${REF##*/}
          docker push ghcr.io/leakytap/tavern-docker:latest
